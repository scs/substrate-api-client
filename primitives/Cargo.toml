[package]
name = "ac-primitives"
version = "0.18.0"
authors = ["Supercomputing Systems AG <info@scs.ch>"]
license = "Apache-2.0"
edition = "2021"
repository = "https://github.com/scs/substrate-api-client"
description = "Substrate-api-client primitive types"
readme = "README.md"
categories = ["no-std"]


[dependencies]
codec = { package = "parity-scale-codec", version = "3.6.1", default-features = false, features = ['derive'] }
impl-serde = { version = "0.4", default-features = false }
primitive-types = { version = "0.12.1", default-features = false, features = ["serde_no_std", "scale-info"] }
scale-info = { version = "2.1.1", default-features = false, features = ["derive"] }
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }

# substrate no_std
sp-core = { default-features = false, features = ["full_crypto", "serde"], version = "34.0.0" }
sp-crypto-hashing = { default-features = false, version = "0.1.0" }
sp-runtime = { default-features = false, features = ["serde"], version = "38.0.0" }
sp-runtime-interface = { default-features = false, version = "28.0.0" }
sp-staking = { default-features = false, features = ["serde"], version = "33.0.0" }
sp-version = { default-features = false, features = ["serde"], version = "36.0.0" }
sp-weights = { default-features = false, features = ["serde"], version = "31.0.0" }
# need to add this for the app_crypto macro
sp-application-crypto = { default-features = false, features = ["full_crypto"], version = "37.0.0" }

# substrate std / wasm only
frame-system = { optional = true, version = "35.0.0" }
pallet-assets = { optional = true, version = "36.0.0" }
pallet-balances = { optional = true, version = "36.0.0" }
pallet-contracts = { optional = true, version = "34.0.0" }
pallet-staking = { optional = true, version = "35.0.0" }

[dev-dependencies]
solochain-template-runtime = { git = "https://github.com/paritytech/polkadot-sdk.git", branch = "release-polkadot-v1.13.0" }
sp-keyring = { version = "38.0.0" }
pallet-transaction-payment = { version = "35.0.0" }

[features]
default = ["std"]
# To support `no_std` builds in non-32 bit environments.
disable_target_static_assertions = [
    "sp-runtime-interface/disable_target_static_assertions",
]
disable-metadata-hash-check = []
std = [
    "codec/std",
    "primitive-types/std",
    "scale-info/std",
    "serde/std",
    "serde_json/std",
    # substrate no_std
    "sp-core/std",
    "sp-crypto-hashing/std",
    "sp-runtime/std",
    "sp-staking/std",
    "sp-version/std",
    "sp-weights/std",
    "sp-application-crypto/std",
    # substrate std
    "frame-system",
    "pallet-assets",
    "pallet-balances",
]
staking-xt = ["std", "pallet-staking"]
contracts-xt = ["std", "pallet-contracts"]
